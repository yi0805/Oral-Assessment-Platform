# Project 20 backend dependencies

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Database
sqlalchemy==2.0.35
psycopg[binary]==3.2.10
alembic==1.13.2
pgvector==0.3.5

# Data validation
pydantic==2.9.0
pydantic-settings==2.5.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AWS
boto3==1.35.0
# Issue #72: AWS Transcribe Streaming SDK. Used by the upcoming
# /sessions/{id}/transcribe/stream WebSocket endpoint to forward PCM
# frames to AWS in real time and receive partial transcripts. Pulls in
# aws-crt and amazon-transcribe-events-* as transitive deps.
amazon-transcribe==0.6.2

# File processing
pandas==3.0.2
python-multipart==0.0.12
pdfplumber==0.11.4
python-docx==1.1.2
python-pptx==1.0.2

# HTTP client
httpx==0.27.0

# Environment
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.27.0
moto[s3]==5.0.0
